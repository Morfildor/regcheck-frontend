// Renamed to ClarificationsPanel. This re-export preserves the old path
// so any stale import (e.g. in snapshots or external tooling) still resolves.
export { default } from "./ClarificationsPanel";
